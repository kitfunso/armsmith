# armsmith workload spec (models.WorkloadSpec).
# Smoke-sized: honest two-stage stats on a short fixed workload. For the
# full submission runs, raise n_prompt/n_gen and confirm_repeats.
n_prompt: 256
n_gen: 64
n_batch: 2048
n_ubatch: 512
screen_repeats: 3
confirm_repeats: 5
eval_text_path: examples/eval.txt
